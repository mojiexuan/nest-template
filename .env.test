# 测试环境配置

NODE_ENV=test
PORT=13001

# 数据库配置（使用测试数据库）
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=chenille_test

# Redis配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT配置
JWT_SECRET=test-secret-key-for-testing-only
JWT_EXPIRES_IN=1h

# 邮件配置（测试环境禁用）
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
